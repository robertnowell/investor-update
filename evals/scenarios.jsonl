{"id": "happy-path", "skills": ["generating-investor-updates"], "query": "Draft our investor update for 2026-05. Cash $410k, burn $28k/mo, ~15mo runway, revenue $2,100/mo ($1.8k SaaS). Brand is Northwind.", "setup": {"env": {"GITHUB_REPO": "owner/repo", "SLACK_TOKEN": "xoxb-..."}, "kopi_mcp": "connected"}, "expected_behavior": ["Runs preflight and compile; reports which sources were used", "Pulls the most recent prior update via the Kopi MCP for MoM deltas", "Distills to a HEADLINE-ONLY brief (no paragraphs)", "Computes Default Dead (revenue << burn) rather than stamping Default Alive", "Calls Kopi:create_email with a referenceEmailUrl from the prior update", "Invents no numbers; every figure traces to the financials or the digest", "Reminds the user to export to the investor list only"]}
{"id": "zero-sources", "skills": ["generating-investor-updates"], "query": "Draft the May investor update. Cash $200k, burn $30k, revenue $0. We're pre-revenue.", "setup": {"env": {}, "kopi_mcp": "connected"}, "expected_behavior": ["preflight reports 0 sources live but does NOT fail or refuse", "Proceeds using founder financials alone; explicitly notes no data sources were available", "Still produces a valid headline-only brief and drafts via Kopi", "Computes Default Dead; does not fabricate shipped-work or growth it has no signal for"]}
{"id": "kopi-missing", "skills": ["generating-investor-updates"], "query": "Generate our investor update for May.", "setup": {"env": {"GITHUB_REPO": "owner/repo"}, "kopi_mcp": "NOT connected"}, "expected_behavior": ["Detects the Kopi MCP / Kopi:create_email is unavailable", "Stops before drafting and tells the user to connect the Kopi MCP (per INSTALL.md)", "Does NOT fabricate an email body to compensate", "May still compile the digest and show it, but does not claim an update was produced"]}
{"id": "byo-adapter-linear", "skills": ["generating-investor-updates"], "query": "Draft the May update. We use Linear, not GitHub, for shipped work.", "setup": {"env": {"LINEAR_API_KEY": "lin_...", "SLACK_TOKEN": "xoxb-..."}, "files": ["adapters/custom/linear.py (renamed from linear.py.example)"], "kopi_mcp": "connected"}, "expected_behavior": ["compile.py auto-discovers the custom Linear adapter with zero SKILL.md edits", "GitHub source is skipped (GITHUB_REPO unset) without error", "Shipped highlights are crystallized from Linear issues_completed + titles", "Produces drafts via Kopi as normal"]}
